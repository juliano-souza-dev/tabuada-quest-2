const test=require("node:test");const assert=require("node:assert/strict");delete globalThis.TabuadaQuest;require("../../web/js/domain/world-structure.js");require("../../web/js/domain/scheduler.js");require("../../web/js/domain/gameplay-session.js");const scheduler=globalThis.TabuadaQuest.domain.scheduler;const gameplay=globalThis.TabuadaQuest.domain.gameplay;
test("sessão usa plano real 22x5",()=>{let session=gameplay.createIslandSession(2,1,"slice");let regionState=gameplay.createRegionState(2);({session,regionState}=gameplay.prepareNextChallenge(session,regionState));assert.equal(session.currentChallenge.regionId,2);assert.equal(session.currentChallenge.islandId,1);assert.equal(session.currentChallenge.globalIslandIndex,6);assert.equal(session.currentChallenge.exposureType,scheduler.EXPOSURE_TYPE.PLANNED)});
test("erro agenda recovery sem consumir exposição extra",()=>{let session=gameplay.createIslandSession(1,1,"recovery");let regionState=gameplay.createRegionState(1);({session,regionState}=gameplay.prepareNextChallenge(session,regionState));const c=session.currentChallenge;({session,regionState}=gameplay.answerCurrentChallenge(session,regionState,-1));assert.equal(session.wrongAnswers,1);assert.equal(session.plannedAnswered,1);assert.equal(regionState.plannedExposureCount,1);assert.equal(regionState.recoveryAttemptCount,0);assert.equal(regionState.recoveryQueue.length,1);assert.equal(regionState.mastery[`${c.table}x${c.multiplier}`].correctStreak,0)});
test("Ilha completa após 20 questões planejadas",()=>{let session=gameplay.createIslandSession(22,4,"full");let regionState=gameplay.createRegionState(22);while(session.phase!=="complete"){if(session.phase==="question"){({session,regionState}=gameplay.prepareNextChallenge(session,regionState));if(session.phase==="complete")break;const c=session.currentChallenge;({session,regionState}=gameplay.answerCurrentChallenge(session,regionState,c.table*c.multiplier))}else{({session,regionState}=gameplay.continueAfterFeedback(session,regionState))}}const result=gameplay.buildResult(session);assert.equal(result.plannedAnswered,20);assert.equal(result.wrongAnswers,0);assert.equal(regionState.plannedExposureCount,20)});
