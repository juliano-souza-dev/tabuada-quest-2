const test=require("node:test");const assert=require("node:assert/strict");global.TabuadaQuest={};require("../../web/js/content/game-content.js");require("../../web/js/domain/world-structure.js");require("../../web/js/domain/player-state.js");const TQ=global.TabuadaQuest;const d=TQ.domain.playerState;const crew=TQ.content.crewMembers;
test("catálogo contém os nove tripulantes reais",()=>{assert.deepEqual(crew.map(x=>x.label),["Atirador","Carpinteiro","Cozinheiro","Espadachim","Explorador","Inventor","Médico","Músico","Navegador"]);assert.equal(new Set(crew.map(x=>x.id)).size,9)});
test("estado v12 inicia sem tripulantes",()=>{const s=d.createInitialState();assert.equal(s.schemaVersion,12);assert.deepEqual(s.crew.hiredIds,[])});
test("contratação desconta ouro uma única vez",()=>{let s=d.createInitialState();s={...s,wallet:{...s.wallet,coins:1000}};const m=crew[0];s=d.hireCrewMember(s,m);assert.equal(s.wallet.coins,1000-m.cost);assert.deepEqual(s.crew.hiredIds,[m.id]);const again=d.hireCrewMember(s,m);assert.equal(again.wallet.coins,s.wallet.coins)});
test("ouro insuficiente não contrata",()=>{let s=d.createInitialState();s={...s,wallet:{...s.wallet,coins:crew[8].cost-1}};assert.deepEqual(d.hireCrewMember(s,crew[8]).crew.hiredIds,[])});
test("bônus acumulam por categoria",()=>{let s=d.createInitialState();s={...s,wallet:{...s.wallet,coins:10000}};for(const id of ["atirador","espadachim","medico"])s=d.hireCrewMember(s,crew.find(x=>x.id===id));const r=d.calculateCrewReward(s,crew,{xp:100,coins:50,gems:10});assert.equal(r.total.xp,116);assert.equal(r.total.coins,50);assert.equal(r.total.gems,10)});
